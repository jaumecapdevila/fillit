import {
    init,
} from './manager';

init();

// browser.storage.local.get()
//     .then((storedSettings) => {
//         if (storedSettings.automaticCheck) {
//             fillForms(findFormsInCurrentView())
//         }
//     })
//     .catch(() => {});
